spring.application.name=Home-Service

# Server Port (React is on 5173, so let's put Spring Boot on 8080)
server.port=8080

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/homeservice_db
spring.datasource.username=root
spring.datasource.password=AbhishekPAL844513@#
# (Change the password above to whatever your MySQL password is)

# Hibernate / JPA Settings (This tells Spring to automatically create your database tables!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
-e 
# JWT Secret Key (Change this in production!)
jwt.secret=HomeServiceSecretKey2024PleaseChangeThisInProduction!
